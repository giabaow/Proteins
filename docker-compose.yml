services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./chroma_data:/app/chroma_data
      - ./reports:/app/reports          # so a regenerated frontend.html shows without a rebuild
